fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.0.0
psycopg[binary]>=3.2.0
# Scraper AAA (promedios estado/metro) — curl_cffi bypasea Cloudflare
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# GasBuddy opcional: NO instalar en Render (rompe el build; PyPI max 0.4.x y Cloudflare)
# pip install "py-gasbuddy>=0.4.2" aiohttp  # solo local si USE_GASBUDDY=1
