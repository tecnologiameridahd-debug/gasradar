# Copia este archivo a .env en CADA VPS:
#   cp .env.example .env
# Pon la MISMA clave en todos los VPS y en Render (VPS_SCRAPER_KEY).

SCRAPER_API_KEY=cambia-esta-clave-larga
CACHE_TTL_SEC=10800

# Nombre de esta máquina (vps1, vps2, vps3...) — solo para logs
SCRAPER_NODE=vps1
